query GetRocketsList($limit: Int, $offset: Int) {
  rockets(limit: $limit, offset: $offset) {
    wikipedia
    type
    success_rate_pct
    name
    description
    diameter {
      meters
    }
  }
}
