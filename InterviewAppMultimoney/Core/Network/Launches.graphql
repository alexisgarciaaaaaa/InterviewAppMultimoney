query GetLaunchesList($offset: Int, $limit: Int) {
  launchesPast(offset: $offset, limit: $limit) {
    mission_name
    launch_year
    launch_success
    details
    id
    links {
      video_link
    }
  }
}
